1. 已登录的用户没有办法切换，只能重登，其实凭证在后台，可以直接切换身份
2. 应用加载后关注的直播需要手动刷新，应该自动在出来，除非网络故障或者没有关注任何人，这个应该是拉起的顺序有问题？
3. 房管功能没有界面？还是没有实现？加黑名单，拦截词那些
4. 历史10条记录前面混入了本地的本人发言记录，仅保留历史记录
5. 历史记录与实时记录存在割裂，保持显示效果的一致性，不提示以上为历史记录
6. 参照官方实现优化一下粉丝牌。身份标识的显示效果，加入发言用户头像，加入时间戳显示开关
7. 人气值好像没什么用，那个参数官方也没实现，现在应该留房间观众数量
8. 优化一下界面布局（可以参照web直播栏）
- 筛选功能全部列到展开菜单中
- 短语的增删界面逻辑修正一下
- 很多可以放到右键的逻辑（举报、@、短语的增删等）尽量放到右键菜单里
- 弹幕前面的时间要对齐（宽度一致）
- 表情菜单栏里，通用表情以外的表情变大一点，好看清楚
- 主站的表情好像还是没办法选择到？
- 字号同步调整表情的尺寸
- 下方菜单展开时，自动把弹幕区域往上弹，不要挡住最新的弹幕
9. 透明度功能非预期的实现方式，先删了，放在待办理
10. 互动（进场消息）增加一个像官方一样能显示一下自动消失的开关，只有常开才一直显示。系统通知默认关闭
11. 关注的未开播也展示出来，按最后开播的时间排序，分页展示


12. 舰长标和本房间的舰长不完全一样，其他房间的舰长也有舰长标
13. @某人 后文本框内会有 @xxx 但是删掉这个后发的弹幕还是会@，另外弹幕里看不到@关系，不知道回复是不是也一样

14. 主界面 竖屏模式时，第一排左对齐头像、主播名 右对齐 直播状态。第二排 左对齐直播标题 右对齐最后开播时间 不要房间号
15. 主界面 宽屏模式时，第一排左对齐头像、主播名、直播状态 右对齐直播状态、最后开播时间 不要房间号

补充，所有宽屏状态下一排展示的内容可以按这个规则在窄屏模式下分成2排，或者一开始就做成2排

16. 主界面 所有关注的主播都需要展示，置顶直播中的直播间、按最近观看的顺序降序排列。
17. 主界面，连接的房间列表不展示房间号，仅展示主播 点 直播间名
18. tab不展示房间号，展示主播名
19. 短语删除颜文字部分
20. 表情界面仿官方实现，使用tab，不要使用按钮，而且通用的表情全部超出了边框，很丑

2609131539
1. 回到最新图标改为下箭头（返回键旋转90度）按钮
2. 不同tab中的界面要做隔离，切换了tab界面也要重新打开，而不是开着界面切换（顺便看看有没有类似的逻辑缺陷）
3. 有房管身份才能有房管界面的选项，这个可以参照官方web的实现方式
4. 切换用户也要做隔离
5. 用户直接点击切换，不要那个专门的切换按钮
6. 手填cookie这个功能直接去掉，现在的登录方式很合理
7. 有一些提示可以不用写，例如：“游客态不登录也能收弹幕；发送需要登录。三种方式：扫码（默认）、手填 Cookie、 退出登录（回到游客态）。”这是写需求时给的案例，不必写在产物程序里，全局检查一下还有没有类似的，能删就删。
8. 短语和筛选顶部的提示和关闭也删掉，展开高度看齐表情界面
9. 关键词命中功能删掉，用不上
10. 切换跟随系统暗色还是亮色的功能放在主界面“弹幕框”右边，全局切换
11. 筛选与布局&快捷短语界面重新做一下布局，要清晰整洁，同时兼顾竖屏和宽屏2种视觉效果
12. 弹幕字数有上限，在输入框限制&提示一下，免得发出去才发现超长了

2609132259
1. 优化一下房管界面整体布局，禁言、黑名单、屏蔽词可以分三个tab
2. 回到最新图标改为下箭头，指这整个按钮删掉，用一个返回按钮旋转90度来代替
3. 添加账号按钮居中，展开的二维码卡片宽度和间距与上方元素保持一致
4. 我同时可以打开房管面板和下面三个面板，这有点太影响布局了，同时只允许打开一个面板，连接到有房管权限的直播间时就加载好房管的数据。移除黑名单。删除等功能可以设置一个批量处理键，然后单点处理绑定在右键
5. 你是本直播间房管和刷新都不需要，打开界面时静默刷新
6. bug：upstream error: 响应解析失败: error decoding response body（UPSTREAM_ERROR）
7. 主题切换模仿安卓或ios的日月按钮，但是分亮、暗、自动三态，是按钮非滑块
8. 账号面板改为直接点击头像所在的那个圆角长方形就能进入，不需要独立的“账号”按钮

2609140651
1. 筛选里的系统和系统通知两项是不是重复了
2. 账号管理里的关闭按钮去掉，这个按钮用不上
3. 房管面板里各tab不需要战术统计数量
4. 房管面板里tab右侧仅留关闭，用X号不用文字
5. 接4，房管面板批量按钮挪到下面的层级，与输入框和添加按钮全部置顶，重新设计一下排布格式，不要竖着排版，很丑，实在不行换成图标也行