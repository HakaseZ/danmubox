{
  // 配置里允许注释（oxlint 支持 jsonc）。
  //
  // 规则口径（2026-09-17 定，与 `AGENT.md` §3/§4 的「Lint 零告警」同一条）：
  //   * `npm run lint` = `oxlint --deny-warnings` —— **告警即失败**，与前端的 clippy 口径对齐；
  //     新增告警要么真修，要么在下表/行内注明理由，`main` 上不允许带告警合入。
  //   * 行内 `// oxlint-disable-next-line <规则名>` 用于**单点**误报（规则看不到意图的那一处），
  //     每一条都必须在同一处写清理由；能整类放行的才写进本文件。
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "plugins": ["react", "typescript", "oxc"],
  "rules": {
    "react/rules-of-hooks": "error",
    "react/only-export-components": ["warn", { "allowConstantExport": true }],
    // 这条规则的前提是 **React Compiler**（它报的是「编译器会跳过这个组件/钩子的记忆化」）。
    // 本仓库没有启用 React Compiler：`vite.config.ts` 只挂了 `@vitejs/plugin-react`，
    // 没有 `babel-plugin-react-compiler`；也就是说规则描述的那个后果在这里不存在。
    // 触发点是 `@tanstack/react-virtual` 的 `useVirtualizer`（`components/MessageList.tsx`），
    // 上游库的 API 形态，不是我们写错了什么。等真引入 React Compiler 时再打开这条。
    "react/incompatible-library": "off"
  },
  "overrides": [
    {
      // `smoke/` 是**前端冒烟夹具**：`room-page.mjs` 的正文是塞在模板字符串里的**页面侧源码**，
      // 反斜杠在模板求值时就被吃掉（`AGENT.md` §9 记着这个坑：写 `/rgba?\(/`，页面里实际是
      // `/rgba?((/`）。文件里那几处 `\(` 出现在**注释文本**中，是「照抄一遍踩过的坑」，
      // 不是可疑的转义 —— 规则按「模板串里的多余转义」判它，属于对这段载荷的误读。
      // 这层载荷本来就该整套换掉（另一票在拆冒烟夹具），所以这里只放行规则、不动文件。
      "files": ["smoke/**"],
      "rules": {
        "eslint/no-useless-escape": "off"
      }
    }
  ]
}
