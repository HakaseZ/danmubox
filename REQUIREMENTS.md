# danmubox 需求基线

> 定位：本项目的**唯一需求来源**，由项目作者手写维护；工程约定由 [`docs/contract.md`](docs/contract.md) 翻译而来。
> 读者：项目作者；以及任何需要确认「这个功能到底做不做」的实现者与 AI 编码 agent。
> 更新时机：增删或修改需求时。需求变更后必须同步 [`docs/contract.md`](docs/contract.md) 的 §9 需求溯源。

| 项 | 值 |
|---|---|
| 项目 | danmubox（弹幕框） |
| 包标识 | `dev.kksk.danmubox` |
| 状态 | 本期范围已确认，无待定项，可进入实现 |
| 最近更新 | 2026-09-11 |

## 1. 目标

复刻 B 站官方直播间的聊天框，**不解码视频**，自用不发布，覆盖 macOS / Windows / 安卓三端。

## 2. 本期功能需求

### 2.1 看弹幕

- 显示弹幕、礼物、醒目留言（SuperChat）、进场与互动提示
- 开播 / 下播 / 标题变更 / 分区变更
- 房间观众数展示：**在线人数**（`ONLINE_RANK_COUNT` 的 `online_count`）与**累计看过**
  （`WATCHED_CHANGE` 的 `num`），两个都显示
  > 2026-09-12 用户反馈：原句是「人气值展示」，但人气值那个参数官方客户端也没实现、没什么用，
  > 界面改为展示上面两个观众数（人气值不再展示；协议侧仍记 `op=3` 的日志）。
- 互动 / 进场消息默认**显示一会儿就自动消失**，可切换为常驻（用户 2026-09-12 反馈）
- 系统通知（开播 / 下播 / 标题变更 / 公告）默认**不显示**，开关打开才显示（用户 2026-09-12 反馈）

### 2.2 发弹幕

- 发送弹幕
- 表情包库：**按用户身份加载**（通用 / 粉丝牌 / 大航海）
  > 2026-09-12 更正：原句还列了「房管」，但**房管没有表情分类**（项目所有者确认；官方前端的表情权限判定
  > `emoticonDanmakuPermCheck` 也只有粉丝团与 1/2/3 总督/提督/舰长两个分支，无房管）。房管身份仍以弹幕徽标体现。
- @某人 / 回复某条弹幕
- 发送失败的原因提示：全局禁言、直播间禁言、粉丝牌等级不足、频率限制
- 输入草稿与最近发送记录：**会话内**保留（不落盘）
- 自定义短语；快捷短语 / 颜文字
- 电池余额

### 2.3 身份与徽标

- 徽标：主播 / 房管 / 舰长 / 提督 / 总督
- 能看到房管身份
- 能看到当前直播间的粉丝牌等级
- 点昵称跳用户主页（跳转浏览器）

### 2.4 举报

- 举报弹幕，行为与官方一致

### 2.5 登录与账号

- 三种方式：游客、手填 Cookie、扫码；**扫码为默认入口**
- Cookie 存进配置文件；启动时本地已有 Cookie 则直接读取
- Cookie 失效时提示重登；扫码换号
- 多账号：**单个配置文件内多 profiles**，可切换

### 2.6 房间与关注

- 房间号 / 短链解析为真实房间
- 登录后能看到所有关注的直播间，**正在直播的置顶**
- 关注列表在会话就绪后**自动加载**，不需要用户手动点刷新；失败时保留「刷新」按钮与错误提示（用户 2026-09-12 反馈）
- 关注分组；可从关注列表直接进场
- 房间内「刷新」按钮：长连接卡住或推流暂时中断时手动重连

### 2.7 礼物

- 能看到礼物事件
- 可配置为**独立礼物栏**，或**混合在弹幕栏中**
- 礼物栏独立滚动
- 连击聚合展示；礼物金额统计与排行

### 2.8 展示与过滤

- 深色模式；字号
  > 2026-09-12 用户反馈：透明度那一版实现方式不是预期，**先删掉**（连带偏好键），重新设计后再说，见 `docs/roadmap.md`
- 关键词过滤与告警（命中时高亮 / 提示音）
- 多房间标签页

### 2.9 数据

- 只保留**单次房内会话**的弹幕：进房间算一次，退出到房间列表再进即刷新

## 3. 架构约束

- B 站 API 不可控，可能有后期的逆向需求：这部分代码必须**完全分离**，后续修改不影响核心业务逻辑

## 4. 非目标（本期明确不做）

| 项 | 说明 |
|---|---|
| 视频流解码 | 只消费弹幕协议，界面里没有播放器 |
| iOS 端、Fold8 / 折叠屏适配 | 后期 enhancement |
| 本地数据库 / 历史落盘 | 见 §2.9，弹幕只在内存中保留 |
| 弹幕回看、导出 | 不做 |
| 词云 | 下期，非核心 |
| AI 原生接口 | 本期置空。想法：后期接入 MCP，让 Agent 直接消费弹幕数据；架构上保持兼容能力即可 |
| 后台保活 / 推送 | 前台运行 |
| 应用商店发布 | 自用产物 |

## 5. 参考与外部输入

| 项 | 说明 |
|---|---|
| 协议实现参考 | 作者 GitHub 上连接并解析 B 站直播数据包的项目（Go 与 Python 各一） |
| 被吞弹幕判定 | [哔哩哔哩直播 被吞弹幕标记](https://update.greasyfork.org/scripts/453468/Bilibili%20Live%20Banned%20Danmaku%20Marker.user.js)：被吞的弹幕划线，平台吞的变红、主播吞的变黄 |
| 包标识 | 由 `dev.zack.danmubox` 改为 `dev.kksk.danmubox` |

## 6. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-11 | 建立需求基线，归并补充场景 |
| 2026-09-11 | 确认无本地数据库、弹幕只做单会话保留、不做回看与导出；AI 需求置空（仅保留后期接入 MCP 的架构兼容）；包标识改为 `dev.kksk.danmubox` |
| 2026-09-11 | 三条待定项定案：展示项仅深色模式 / 字号 / 透明度三项；草稿与最近发送记录为会话内；多账号为单文件多 profiles |
| 2026-09-12 | 用户反馈落地：人气值改为房间观众数（在线人数 + 累计看过）；互动消息可自动消失（默认开）、系统通知默认关；关注列表自动加载；透明度功能先删除（见 `docs/roadmap.md`） |
